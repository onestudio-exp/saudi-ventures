version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/db/schema"
    queries: "internal/db/queries"
    gen:
      go:
        package: "db"
        out: "internal/db/gen"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types: true
        rename:
          image_url: "ImageURL"
          logo_url: "LogoURL"
        overrides:
          - db_type: "datetime"
            go_type: "time.Time"
